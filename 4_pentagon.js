// Draw a pentagon
pendown()
for( i=0; i<7; i++ ){
    forward( 50 )
    right( 72 )
}

// Draw a octagon
pendown()
for( i=0; i<9; i++ ){   // See change here?
    forward( 50 )
    right( 45 )         // and here?
}